<!DOCTYPE html><html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>BOT-HOSTING – Create Server</title>
  <!-- Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    /* smooth fonts */
    body { -webkit-font-smoothing: antialiased; -moz-osx-font-smoothing: grayscale; }
  </style>
  <!-- PayPal JS SDK: IMPORTANT -> replace YOUR_PAYPAL_CLIENT_ID below with your real client id -->
  <script src="https://www.paypal.com/sdk/js?client-id=AVTymlwY1YBOFuRWD5rkTANeLHxMA_X7kENSxYZVf3ydVCQ1YHa_wCdJDpNTfZXBZXzRhB036TFfcSgu&components=buttons&currency=USD"></script>
</head>
<body class="bg-gray-900 text-white">
  <div class="flex min-h-screen">
    <!-- Sidebar -->
    <aside class="w-64 bg-gray-800 p-6 flex flex-col justify-between">
      <div>
        <div class="text-2xl font-bold tracking-wide mb-8">BOT-HOSTING.NET</div>
        <nav class="space-y-4">
          <a href="#create" class="block hover:text-blue-400">Create Server</a>
          <a href="#manage" class="block opacity-60 hover:opacity-100">Manage Servers (draft)</a>
          <a href="#panel" class="block opacity-60 hover:opacity-100">Server Panel (draft)</a>
        </nav>
      </div>
      <div class="mt-10">
        <h2 class="text-sm uppercase tracking-widest text-gray-400 mb-2">Help & Support</h2>
        <a
          href="https://discord.gg/e34qHR6zrq"
          target="_blank"
          class="inline-flex items-center gap-2 px-3 py-2 rounded-lg bg-gray-700 hover:bg-gray-600"
        >
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="w-5 h-5"><path d="M20.317 4.369A19.791 19.791 0 0016.558 3c-.2.36-.43.85-.59 1.23a18.27 18.27 0 00-7.937 0A7.68 7.68 0 007.441 3a19.87 19.87 0 00-3.76 1.37C1.52 8.051.748 11.59 1.04 15.08a19.9 19.9 0 005.993 2.99c.48-.66.91-1.36 1.28-2.09-.7-.26-1.37-.59-2.01-.97.17-.12.33-.25.49-.38 3.87 1.8 8.07 1.8 11.92 0 .17.13.33.26.5.38-.64.38-1.31.71-2.01.97.37.73.8 1.43 1.28 2.09a19.88 19.88 0 005.99-2.99c.3-3.48-.48-7.03-2.19-10.71zM9.68 13.61c-1.15 0-2.09-1.06-2.09-2.37 0-1.3.92-2.37 2.09-2.37 1.16 0 2.1 1.07 2.1 2.37 0 1.31-.94 2.37-2.1 2.37zm4.64 0c-1.16 0-2.1-1.06-2.1-2.37 0-1.3.94-2.37 2.1-2.37 1.17 0 2.09 1.07 2.09 2.37 0 1.31-.92 2.37-2.09 2.37z"/></svg>
          Discord
        </a>
      </div>
    </aside><!-- Main Content -->
<main class="flex-1 p-8" id="create">
  <h1 class="text-3xl font-extrabold mb-6">Create Server</h1>

  <!-- Step 1: Server form -->
  <section class="bg-gray-800 rounded-2xl p-6 shadow-xl max-w-xl">
    <div class="mb-4">
      <label class="block text-sm mb-1" for="serverName">Server name<span class="text-red-500">*</span></label>
      <input id="serverName" type="text" maxlength="30" placeholder="Enter your server name" class="w-full p-3 rounded-lg bg-gray-700 outline-none focus:ring-2 focus:ring-blue-500"/>
      <p class="text-xs text-gray-400 mt-1">30 characters maximum</p>
    </div>

    <div class="mb-6">
      <label class="block text-sm mb-1" for="language">Select the Discord Bot language<span class="text-red-500">*</span></label>
      <select id="language" class="w-full p-3 rounded-lg bg-gray-700 outline-none focus:ring-2 focus:ring-blue-500">
        <option>Node.js</option>
        <option>Python</option>
        <option>Java</option>
      </select>
    </div>

    <button id="nextBtn" class="w-full bg-blue-600 hover:bg-blue-500 transition rounded-lg py-3 font-semibold">Next</button>
  </section>

  <!-- Step 2: Plans (hidden until Next) -->
  <section id="plans" class="hidden mt-10">
    <h2 class="text-2xl font-bold mb-4">Select your plan</h2>
    <p class="text-gray-400 mb-6">Monthly pricing. Coins & weekly options removed.</p>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      <!-- $1 plan -->
      <div class="bg-gray-800 rounded-2xl p-6 shadow-xl flex flex-col">
        <h3 class="text-xl font-bold mb-1">Starter</h3>
        <ul class="text-gray-300 text-sm space-y-1 mb-4">
          <li>RAM: 256 MB</li>
          <li>CPU: 20%</li>
          <li>Storage: 1024 MB</li>
        </ul>
        <div class="text-2xl font-extrabold mb-3">$1<span class="text-base font-normal">/month</span></div>
        <div id="paypal-btn-1" class="mt-auto"></div>
      </div>

      <!-- $3 plan -->
      <div class="bg-gray-800 rounded-2xl p-6 shadow-xl flex flex-col">
        <h3 class="text-xl font-bold mb-1">Starter+</h3>
        <ul class="text-gray-300 text-sm space-y-1 mb-4">
          <li>RAM: 512 MB</li>
          <li>CPU: 25%</li>
          <li>Storage: 2048 MB</li>
        </ul>
        <div class="text-2xl font-extrabold mb-3">$3<span class="text-base font-normal">/month</span></div>
        <div id="paypal-btn-3" class="mt-auto"></div>
      </div>

      <!-- $5 plan -->
      <div class="bg-gray-800 rounded-2xl p-6 shadow-xl flex flex-col">
        <h3 class="text-xl font-bold mb-1">Advanced</h3>
        <ul class="text-gray-300 text-sm space-y-1 mb-4">
          <li>RAM: 1024 MB</li>
          <li>CPU: 50%</li>
          <li>Storage: 3072 MB</li>
        </ul>
        <div class="text-2xl font-extrabold mb-3">$5<span class="text-base font-normal">/month</span></div>
        <div id="paypal-btn-5" class="mt-auto"></div>
      </div>
    </div>

    <p class="text-xs text-gray-400 mt-6">After payment you will be redirected to a success page. Server creation will be manual for now (we'll automate later).</p>
  </section>

  <!-- Draft anchors for sidebar links -->
  <section id="manage" class="mt-20 opacity-70">
    <h2 class="text-2xl font-bold mb-2">Manage Servers (Draft)</h2>
    <p class="text-gray-400">Coming soon: list of your active servers, start/stop, resource usage.</p>
  </section>

  <section id="panel" class="mt-12 opacity-70">
    <h2 class="text-2xl font-bold mb-2">Server Panel (Draft)</h2>
    <p class="text-gray-400">Coming soon: login to the control panel to view console & logs.</p>
  </section>
</main>

  </div>  <script>
    // State for the form
    const serverNameEl = document.getElementById('serverName');
    const languageEl = document.getElementById('language');
    const plansEl = document.getElementById('plans');
    const nextBtn = document.getElementById('nextBtn');

    // Show plans after validating form
    nextBtn.addEventListener('click', () => {
      const name = serverNameEl.value.trim();
      const lang = languageEl.value;
      if (!name) {
        alert('Please enter a server name');
        return;
      }
      // reveal plans
      plansEl.classList.remove('hidden');
      // Scroll to plans
      plansEl.scrollIntoView({ behavior: 'smooth' });
      // (Re)render PayPal buttons with current metadata
      renderPayPalButtons(name, lang);
    });

    // Render PayPal buttons for each plan amount
    function renderPayPalButtons(serverName, language) {
      const meta = `${serverName} | ${language}`; // used in custom_id so you can see what was purchased

      // Helper to mount a button to a container with fixed amount
      const mountButton = (containerId, amountUsd) => {
        const container = document.getElementById(containerId);
        container.innerHTML = ''; // reset if re-rendering

        if (!window.paypal) {
          container.innerHTML = '<div class="text-red-400 text-sm">PayPal SDK failed to load. Check your client-id.</div>';
          return;
        }

        paypal.Buttons({
          style: { layout: 'vertical', color: 'blue', shape: 'rect', label: 'paypal' },
          createOrder: function(data, actions) {
            return actions.order.create({
              purchase_units: [{
                description: `Hosting Plan $${amountUsd}/mo`,
                custom_id: meta,
                amount: { currency_code: 'USD', value: amountUsd.toFixed(2) }
              }],
              application_context: { shipping_preference: 'NO_SHIPPING' }
            });
          },
          onApprove: function(data, actions) {
            return actions.order.capture().then(function(details) {
              alert(`Payment successful! Thanks ${details.payer.name.given_name}.\nOrder: ${details.id}`);
              // TODO: send details to your backend to provision access
              window.location.href = '/success.html';
            });
          },
          onError: function(err) {
            console.error(err);
            alert('Payment failed. Please try again.');
          }
        }).render('#' + containerId);
      };

      mountButton('paypal-btn-1', 1);
      mountButton('paypal-btn-3', 3);
      mountButton('paypal-btn-5', 5);
    }
  </script></body>
</html>
